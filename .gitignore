# --- Data (too large for git; download from Kaggle) ---
data/*.csv
data/*.parquet
*.zip

# --- Environments ---
.venv/
venv/
env/
.conda/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Streamlit ---
.streamlit/secrets.toml

# --- OS / editor noise ---
.DS_Store
Thumbs.db
.vscode/

# --- Model artifact ---
# The trained model is small, so it is committed by default so the app loads
# after cloning. To regenerate it instead, uncomment the next line:
# model/*.joblib